# 우리 반 MUD — 2026-09-23 업데이트

이번 수정은 `games/mudgame`의 로컬 소스에 반영했습니다. 운영 중인 Supabase와 Vercel에는 자동 배포하지 않았습니다.

## 기존 서비스에 적용하는 순서

이미 001·002 SQL과 `student-access` Edge Function을 적용한 서비스 기준입니다.

1. 교사 운영 설정에서 게임을 잠시 일시정지합니다.
2. Supabase SQL Editor에서 아래 파일 전체를 각각 순서대로 실행합니다. 각 파일은 하나의 트랜잭션입니다. 오류가 나오면 다음 파일로 넘어가지 말고 해당 오류부터 해결합니다.
   - `supabase/migrations/202609230003_creation_expansion.sql`: NPC·책·마을 건축물·창작 비용·추가 보상·가방 테이블.
   - `supabase/migrations/202609230004_adventure.sql`: 탐험·대화·독서·전투·전리품·장비·레벨 성장.
   - `supabase/migrations/202609230005_starter_world.sql`: 기본 세계·몬스터·아이템·NPC·책·문제.
3. 수정된 이 프로젝트를 기존 Vercel 배포 경로로 반영하고 다시 배포합니다. Build command는 `npm run build`, output directory는 `dist`입니다. 소스와 함께 새 `public/mud-icon.png`도 포함합니다.
4. 기존 `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` 환경변수를 그대로 유지합니다. 이번 변경에는 새로운 환경변수나 Secret이 없습니다. Edge Function 재업로드도 필요하지 않습니다.
5. 교사 계정의 세계 관리에서 기본 장소와 NPC·책 목록을 확인합니다. 운영 설정에서 확률·보상 한도를 확인하고 일시정지를 해제합니다.
6. 학생으로 새로고침하여 광장에서 `동`, 전투 후 `가방`, 별빛 마을에서 `입장` → `읽기`를 확인합니다. `동`을 3회 이동하면 별빛 마을이며, 이동 중에는 전투가 발생할 수 있습니다.

001·002를 다시 실행할 필요는 없습니다. 003·004는 한 번씩 적용하는 스키마 변경입니다. 005는 고정 ID로 기본 데이터를 넣으며 재실행 시 기존 레코드를 덮어쓰지 않습니다. 처음 만드는 새 DB라면 001부터 005까지 순서대로 적용합니다. 기존 계정·문제·잔액·제작물을 지우지 않습니다.

이전에 신청한 주택·치유소·도서관은 연계 마을 정보가 없을 수 있습니다. 해당 신청에는 수정 요청을 보내고, 학생이 연계 마을을 선택하여 다시 제출하게 합니다. 예전 아이템·기술 데이터에 폼의 숨겨진 몬스터 능력치가 섞여 있었으므로 004는 기존 아이템·기술의 HP/공격/방어를 0으로 정규화합니다. 기존 효과와 강도는 유지됩니다.

## 추가된 기능과 규칙

- 창작 종류: 맵, NPC, 책, 몬스터, 아이템, 기술. 맵 외에는 칸 수·장소 성격·연결 장소·방향을 비활성화합니다.
- NPC 설명의 줄바꿈은 대사 구분으로 표시됩니다. 학생 NPC는 제작자 소유의 활성 마을에만 무작위 등장하며, 누구나 방문해서 대화할 수 있습니다. 마을이 없는 제작자의 NPC는 등장하지 않습니다. 기본 마을·성에는 기본 NPC가 등장합니다.
- 주택·치유소·도서관은 자신의 마을을 드롭다운에서 선택합니다. 마을 한 칸당 활성 건축물 하나이며 신청 중에도 같은 칸을 중복 예약할 수 없습니다. `입장`, `나가기`로 이동합니다.
- 책의 설명이 본문입니다. 모든 도서관은 전체 승인된 책 중 하나를 무작위로 제시합니다. 같은 책은 학생별·한국 시간 날짜별로 한 번만 보상 추첨합니다. 재방문하면 다른 책이 선택될 수 있습니다.
- 아이템은 무기·방어구·장신구·소모품으로 나눕니다. 공격력·방어력·체력 및 데미지·회복·방어 효과를 지정합니다. 장비는 각 슬롯당 하나만 착용하지만 가방 보유 개수에는 제한이 없습니다.
- 아이템 제작 승인 자체는 소지품 지급이 아닙니다. 승인된 아이템은 공통 전리품 목록에 들어가고 몬스터·보물상자에서 획득합니다. 기술은 만든 학생이 사용할 수 있습니다.
- 몬스터는 HP·공격·방어·레벨·최소 정답 횟수·출현 지역·과목·특수 효과를 지정합니다. 숲·황야, 길·다리, 던전에서 무작위로 만납니다. 보스방 분류는 삭제하고 기존 보스방은 던전으로 전환합니다.
- 공격 피해는 정답일 때만 발생합니다. 틀리면 반격을 받고, 해설을 읽고 다음 문제에 도전합니다. 최소 정답 횟수를 채우기 전에는 강한 공격으로도 한 번에 처치되지 않습니다.
- 전투 중 회복·데미지 효과는 정답에, 방어 효과는 오답의 반격에 적용됩니다. 회복 소모품은 전투 밖에서도 `사용 번호`로 사용합니다. 장비 능력치는 착용 중에 합산됩니다.
- 처치 경험치는 `최대(10, 몬스터 레벨×10)`이며 100 EXP마다 레벨이 오릅니다. 레벨당 최대 체력 +5, 공격·방어 +1, 체력 회복입니다.
- 문제·창작 검토에 추가 창조력 0~100 입력란을 제공합니다. 같은 제출물에 추가 보상은 한 번만 지급하며 교사가 승인할 때만 반영됩니다. 문제 일괄 승인에서는 문제마다 입력한 추가 보상을 적용합니다. 추가 보상은 문제 기본 보상의 일일 한도와 별개입니다.
- 창작 검토에 새로고침, 세계 관리에 NPC·책 일괄 관리 및 편집을 추가했습니다.
- 탐험은 PowerShell 스타일 터미널입니다. 전체화면/Esc, 명령 기록 ↑↓, DOS 형태 출구 목록, 순차 텍스트 표시, 도움말·가방을 제공합니다. 오른쪽 빠른 행동 패널은 제거했습니다.

## 창조력 비용

| 종류 | 비용 |
|---|---|
| 맵 | 칸 수 × 칸당 비용(기본 10) |
| 몬스터 | 기본 비용 + 올림(HP/10) + 공격 + 방어 + 레벨×2 + 효과 강도×2 |
| 아이템 | 기본 비용 + 올림(HP/5) + 공격×2 + 방어×2 + 효과 강도×2 |
| 기술 | 기본 비용 + 효과 강도×2 |
| NPC·책 | 기본 비용 + 500자를 넘는 500자 구간마다 1 |

화면과 서버에서 같은 계산을 사용합니다. 서버가 능력치 범위와 잔액을 검증하고, 신청 시 예약 → 승인 시 차감합니다. 취소·반려·수정 요청은 예약을 해제합니다.

## 기본 세계와 운영 기본값

장소 21곳(기존 시작 광장 포함), 몬스터 5종, 아이템 6종, NPC 3명, 책 3권, 문제 16개입니다. 기본 문제는 교사 검토 목록에서 확인할 수 있으며 학급 진도에 맞지 않으면 출제를 중지하거나 편집할 수 있습니다.

광장을 중심으로 회복 장소, 마을, 도서관, 숲과 동굴이 연결되어 있어 창조력 없이 시작할 수 있습니다. 외곽에는 3칸과 2칸짜리 단절 지역이 있습니다. 맵 확장의 마지막 칸이 기존 장소와 만날 경우 출구가 비어 있으면 연결합니다. 기존 장소의 이름·소유자는 바꾸지 않습니다. 연결 칸도 신청 칸 수에 포함됩니다. 이미 사용 중인 출구는 기본 데이터가 덮어쓰지 않습니다.

| 항목 | 기본값 | 교사 설정 |
|---|---|---|
| 이동 중 몬스터 조우 | 40% | 가능 |
| 처치 시 아이템 획득 | 40% | 가능 |
| 처치 시 창조력 | 45% 확률로 1~2 | 일일 총한도 조정 |
| 책 보상 | 30% 확률로 +1 | 확률 조정 가능 |
| 탐험 창조력 총한도 | 학생별 하루 10 | 0~100 |
| 이동 중 보물상자 | 대상 지역에서 20% | SQL 기본 규칙 |
| 상자 보상 | 무작위 아이템 + 창조력 1 | 장소별 하루 한 번, 창조력 총한도 적용 |

보상 판정과 인벤토리는 서버에서 관리하며 요청 재전송으로 중복 지급되지 않습니다. 독서·상자 반복 조작으로 문제 제작보다 쉽게 창조력이 쌓이지 않도록 제한을 두었습니다.

## 검증

`node --test tests/expansion.test.mjs tests/database.test.mjs tests/forms.test.ts`

PGlite의 로컬 PostgreSQL에서 001~005 적용, 소유권·승인·보상 중복·건축물 제한·전투·전리품·1,000개 초과 가방·장비·책·외곽 길 연결을 검증합니다. 실제 운영 DB를 변경하는 테스트가 아닙니다.

`node node_modules/typescript/bin/tsc --noEmit`

`npm run build`

로컬 Vite를 5173에서 실행한 뒤 `node tests/browser.mjs`로 교사/학생 미리보기, 종류별 입력, 보너스 입력, 전체화면, 모바일 한 줄 입력과 가방을 검증합니다. 실제 로그인 세션의 운영 서비스 검증은 SQL 적용과 배포 후 수행해야 합니다.

## 후속 제안

1. 학생에게 직접 방문한 지역만 보이는 탐험 지도를 추가하면 길을 이어가는 동기가 더 분명해집니다.
2. 교사가 수업별 사용 문제 묶음을 선택할 수 있게 하면 기본 문제와 학생 문제를 진도에 맞춰 운영하기 좋습니다.
3. 몬스터 반복 사냥 보상보다 좋은 해설·좋은 책에 대한 교사 보너스를 중심으로 운영하는 것을 권합니다.

앞의 기능은 구현했고, 이 세 항목은 후속 제안입니다.

## 아이콘 생성 기록

최종 파일: `public/mud-icon.png`. 앱 왼쪽 위 로고와 favicon에서 사용합니다. 내장 image_gen 도구로 생성했으며 CLI는 사용하지 않았습니다.

최종 생성 프롬프트:

> Use case: logo-brand. Asset type: square app icon for a Korean classroom educational text-adventure MUD, used at 44px in a sidebar. Create a beautifully crafted, bold pixel-art emblem: an open ivory book whose pages form a tiny emerald castle doorway, with a small warm golden star above it, on a rich dark forest-green rounded square background filling the image. Nostalgic 1990s RPG / terminal adventure feel, crisp chunky pixel geometry, subtle dimensional shading, highly readable silhouette, centered with generous inner padding. No text, no letters, no watermark. One single finished app icon, not a presentation sheet. Square 1024x1024.
